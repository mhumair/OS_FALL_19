#include <stdio.h>// Preprocessor Directive

int main(int argc, char *argv) // The range of valid C program main signatures are many. 
{
       /*
        *   printf - format and print data to standard output .
        */
       printf("Hello, World\n ");
       
       /*
        Whether "return 0;" should be omitted or not is open to debate. 
        */
       return 0;
}
